//- list.pug
extends layout.pug

block title
  - var title = __( 'list-title', __( prop + '-label' ), __( prop + '-' + value ) )
  - var description = __( prop + '-' + value + '-description' )
  title!= title + " — " + __( 'title' )
  meta( name="description", content= description )/

block social
  meta( property="og:title", content= title )/
  meta( property="og:description", content= description )/

block scripts
  script( src="/js/pt.min.js" )

block styles
  link( rel="stylesheet", href="/css/list.css" )/
  link( rel="stylesheet", href="/css/microtable.css" )/
  link( rel="stylesheet", href="/css/legend.css" )/

block content
  div.pt-page.pt-box.pt-list
    h1!= title
    p.pt-page-intro!= description
    include templates/element-list.pug
  div.pt-page
    include templates/microtable.pug
    include templates/legend.pug