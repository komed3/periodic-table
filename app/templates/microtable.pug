//- templates/microtable.pug
div.pt-microtable
  each el, k in elements
    a(
      href='/e/' + el.symbol,
      style='--g:' + el.column + ';--p:' + el.period,
      class='pt-el' + (
        el.symbol == element.symbol || ''
          ? ' current'
          : ''
      )
    )
      span= el.symbol