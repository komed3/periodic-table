const express=require("express"),routes=require("./config/routes.min"),app=express();routes.routes.forEach((e=>{app.get(e[0],((s,r)=>{try{var t=require("./app/"+e[1]+".min");r.status(e[2]||200).send(t.out(s,e))}catch(e){r.status(500).send("ERROR: "+e)}}))}));const server=app.listen(3e3);