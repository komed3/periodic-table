const express=require("express"),routes=require("./config/routes.min"),app=express();app.use("/css",express.static("public/styles")),app.use("/js",express.static("public/scripts")),app.use("/img",express.static("public/images")),routes.routes.forEach((s=>{app.get(s[0],((e,p)=>{try{var t=require("./app/"+s[1]+".min");p.status(s[2]||200).send(t.out(e,s))}catch(s){p.status(500).send("ERROR: "+s)}}))}));const server=app.listen(3e3);